# gateway-loadbalancer

Gateway + reverse proxy + round-robin backend pool (simulasi distributed system).

## Setup
1. Copy `.env.example` -> `.env` lalu isi IP/port sesuai laptop.
2. Install:
   ```bash
   npm i
